% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_fixed_blocking.R
\name{DesignFixedBlocking}
\alias{DesignFixedBlocking}
\title{A stratified blocking Fixed Design}
\description{
An R6 Class encapsulating the data and functionality for a fixed stratified
blocking experimental design.
}
\examples{
des = DesignFixedBlocking$new(n = 20, response_type = 'continuous', strata_cols = 'x2', equal_block_sizes = FALSE)
X = data.frame(x1 = rnorm(20), x2 = factor(rep(1:2, 10)))
des$add_all_subjects_to_experiment(X)
des$assign_w_to_all_subjects()
}
\section{Super classes}{
\code{Design} -> \code{DesignBlocking} -> \code{DesignMatching} -> \code{DesignFixed} -> \code{DesignFixedBlocking}
}
\section{Methods}{
\subsection{Public methods}{
  \itemize{
    \item \href{#method-DesignFixedBlocking-initialize}{\code{DesignFixedBlocking$new()}}
    \item \href{#method-DesignFixedBlocking-draw_ws_according_to_design}{\code{DesignFixedBlocking$draw_ws_according_to_design()}}
    \item \href{#method-DesignFixedBlocking-clone}{\code{DesignFixedBlocking$clone()}}
  }
}
\if{html}{\out{<details><summary>Inherited methods</summary>
<ul>
  <li><code>Design$add_one_subject_response()</code></li>
  <li><code>Design$any_censoring()</code></li>
  <li><code>Design$assert_all_responses_recorded()</code></li>
  <li><code>Design$assert_all_subjects_arrived()</code></li>
  <li><code>Design$assert_even_allocation()</code></li>
  <li><code>Design$assert_fixed_sample()</code></li>
  <li><code>Design$check_experiment_completed()</code></li>
  <li><code>Design$duplicate()</code></li>
  <li><code>Design$get_X()</code></li>
  <li><code>Design$get_X_imp()</code></li>
  <li><code>Design$get_X_raw()</code></li>
  <li><code>Design$get_dead()</code></li>
  <li><code>Design$get_missingness_method()</code></li>
  <li><code>Design$get_model_formula()</code></li>
  <li><code>Design$get_n()</code></li>
  <li><code>Design$get_ordinal_levels()</code></li>
  <li><code>Design$get_original_ordinal_levels()</code></li>
  <li><code>Design$get_prob_T()</code></li>
  <li><code>Design$get_response_type()</code></li>
  <li><code>Design$get_response_type_original()</code></li>
  <li><code>Design$get_t()</code></li>
  <li><code>Design$get_w()</code></li>
  <li><code>Design$get_y()</code></li>
  <li><code>Design$get_y_original()</code></li>
  <li><code>Design$has_covariates()</code></li>
  <li><code>Design$is_fixed_sample_size()</code></li>
  <li><code>Design$transform_y()</code></li>
  <li><code>DesignBlocking$add_all_subject_matched_pair_ids()</code></li>
  <li><code>DesignBlocking$assert_blocking_design()</code></li>
  <li><code>DesignBlocking$assert_equal_block_sizes()</code></li>
  <li><code>DesignBlocking$get_block_ids()</code></li>
  <li><code>DesignBlocking$get_cmh_se_w_mat()</code></li>
  <li><code>DesignBlocking$inject_cmh_se_w_mat()</code></li>
  <li><code>DesignBlocking$is_blocking_design()</code></li>
  <li><code>DesignBlocking$is_complete_blocking_design()</code></li>
  <li><code>DesignBlocking$set_m()</code></li>
  <li><code>DesignBlocking$summarize_blocks()</code></li>
  <li><code>DesignMatching$assert_matching_design()</code></li>
  <li><code>DesignMatching$get_matching_cluster_ids()</code></li>
  <li><code>DesignMatching$is_matching_design()</code></li>
  <li><code>DesignFixed$add_all_subject_responses()</code></li>
  <li><code>DesignFixed$add_all_subjects_to_experiment()</code></li>
  <li><code>DesignFixed$assign_w_to_all_subjects()</code></li>
  <li><code>DesignFixed$overwrite_all_subject_assignments()</code></li>
  <li><code>DesignFixed$supports_resampling()</code></li>
</ul>
</details>}}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DesignFixedBlocking-initialize"></a>}}
\if{latex}{\out{\hypertarget{method-DesignFixedBlocking-initialize}{}}}
\subsection{\code{DesignFixedBlocking$new()}}{
  Initialize a fixed stratified blocking experimental design
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{DesignFixedBlocking$new(
  strata_cols = NULL,
  response_type,
  prob_T = 0.5,
  include_is_missing_as_a_new_feature = TRUE,
  n = NULL,
  preferred_num_bins_for_continuous_covariate = 2,
  B_target = if (!is.null(n)) max(1L, floor(sqrt(n))) else NA_integer_,
  exact_num_blocks = FALSE,
  equal_block_sizes = TRUE,
  m = NULL,
  verbose = FALSE,
  missingness_method = "impute",
  model_formula = ~.,
  seed = NULL
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{strata_cols}}{A character vector of column names to use for stratification.
If `NULL` (the default), all available covariate columns are used.}
      \item{\code{response_type}}{"continuous", "incidence", "proportion", "count", "survival", or
"ordinal".}
      \item{\code{prob_T}}{Probability of treatment assignment.}
      \item{\code{include_is_missing_as_a_new_feature}}{Flag for missingness indicators.}
      \item{\code{n}}{The sample size.}
      \item{\code{preferred_num_bins_for_continuous_covariate}}{The number of quantile bins to use for continuous strata. Default is 2.}
      \item{\code{B_target}}{The target number of blocks. Columns from `strata_cols`
are added greedily in order, each column being included only if it does not push
the total number of unique blocks beyond this target. For categorical covariates
their natural levels are used; for continuous covariates
`preferred_num_bins_for_continuous_covariate` quantile bins are used. Earlier columns
are always preferred over later ones. The default is `floor(sqrt(n))` when `n`
is known at construction time, or is resolved to `floor(sqrt(n))` when subjects
are added. Set `B_target = NULL` to use all columns unconditionally.
Set `exact_num_blocks = TRUE` to hard fail if the final key construction
does not produce exactly `B_target` blocks.}
      \item{\code{exact_num_blocks}}{Whether to require the greedy key construction to produce
exactly `B_target` blocks. Default `FALSE`.}
      \item{\code{equal_block_sizes}}{Whether to require all blocks to have the same number of
subjects. Default `TRUE`. When `TRUE` and both `n` and `B_target` are known at
construction time, an error is raised immediately if `n` is not divisible by
`B_target`. A second check fires when subjects are added: if the covariate-based
strata produce unequal block counts the design errors at that point. Set to
`FALSE` to allow unequal blocks (note that `InferenceIncidCMH` and
`InferenceIncidExtendedRobins` still require equal block sizes regardless).}
      \item{\code{m}}{Optional integer vector of explicit block identifiers, one per subject.
If supplied, `n` must also be supplied and `length(m)` must equal `n`.
The constructor then records this blocking structure immediately via
`set_m()`, bypassing covariate-derived strata construction.}
      \item{\code{verbose}}{A flag for verbosity.}
      \item{\code{missingness_method}}{How to handle missing values in covariates.}
      \item{\code{model_formula}}{A formula object.}
      \item{\code{seed}}{Integer seed for reproducibility.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A new `DesignFixedBlocking` object
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DesignFixedBlocking-draw_ws_according_to_design"></a>}}
\if{latex}{\out{\hypertarget{method-DesignFixedBlocking-draw_ws_according_to_design}{}}}
\subsection{\code{DesignFixedBlocking$draw_ws_according_to_design()}}{
  Draw multiple treatment assignment vectors according to stratified blocking.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{DesignFixedBlocking$draw_ws_according_to_design(r = 100)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{r}}{The number of designs to draw.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A matrix of size n x r.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DesignFixedBlocking-clone"></a>}}
\if{latex}{\out{\hypertarget{method-DesignFixedBlocking-clone}{}}}
\subsection{\code{DesignFixedBlocking$clone()}}{
  The objects of this class are cloneable with this method.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{DesignFixedBlocking$clone(deep = FALSE)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{deep}}{Whether to make a deep clone.}
    }
    \if{html}{\out{</div>}}
  }
}

}
