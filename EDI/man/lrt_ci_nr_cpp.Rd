% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{lrt_ci_nr_cpp}
\alias{lrt_ci_nr_cpp}
\title{LRT confidence interval by Newton-Raphson + bisection (Rcpp implementation)}
\usage{
lrt_ci_nr_cpp(
  fit_null_fn,
  neg_loglik_fn,
  score_fn,
  est,
  full_negloglik,
  alpha,
  step,
  lower_seed,
  upper_seed,
  j,
  max_bracket = 60L,
  max_nr_iter = 25L,
  tol_p = 1e-07,
  tol_bracket = 1e-08
)
}
\arguments{
\item{fit_null_fn}{R function \code{delta -> list} (constrained null fit)}

\item{neg_loglik_fn}{R function \code{fit -> double} (negative log-likelihood)}

\item{score_fn}{R function \code{fit -> numeric} (score vector)}

\item{est}{Point estimate of the treatment effect}

\item{full_negloglik}{Negative log-likelihood of the unrestricted model}

\item{alpha}{Significance level (e.g. 0.05)}

\item{step}{Initial step size for exponential bracket search}

\item{lower_seed}{Initial lower-bound candidate, typically the Wald lower CI}

\item{upper_seed}{Initial upper-bound candidate, typically the Wald upper CI}

\item{j}{1-indexed position of the treatment coefficient in the
score vector}

\item{max_bracket}{Maximum exponential bracket search iterations (default 60)}

\item{max_nr_iter}{Maximum NR+bisection iterations per bound (default 25)}

\item{tol_p}{P-value convergence tolerance (default 1e-7)}

\item{tol_bracket}{Bracket-width convergence tolerance (default 1e-8)}
}
\value{
Unnamed numeric vector of length 2: \code{[lower_bound, upper_bound]}
}
\description{
Implements the bracket search + NR+bisection loop entirely in C++, calling
back into R only for \code{fit_null_fn}, \code{neg_loglik_fn}, and
\code{score_fn}. The derivative \eqn{dp/d\delta = 2 f_{\chi^2}(T) \cdot
\mathrm{score}[j]} follows from the envelope theorem.
}
