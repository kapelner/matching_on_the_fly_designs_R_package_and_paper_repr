% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{pval_invert_ci_cpp}
\alias{pval_invert_ci_cpp}
\title{CI inversion by p-value bracket search + bisection (Rcpp implementation)}
\usage{
pval_invert_ci_cpp(
  pval_fn,
  est,
  alpha,
  step,
  lower_seed,
  upper_seed,
  max_bracket = 60L,
  max_bisect = 60L,
  tol = 1e-06
)
}
\arguments{
\item{pval_fn}{R function \code{delta -> double} two-sided p-value}

\item{est}{Point estimate of the treatment effect}

\item{alpha}{Significance level (e.g. 0.05)}

\item{step}{Initial step size for exponential bracket search}

\item{lower_seed}{Wald lower CI bound (used as first bracket candidate;
pass \code{NA_real_} to skip)}

\item{upper_seed}{Wald upper CI bound (same)}

\item{max_bracket}{Maximum exponential bracket search iterations (default 60)}

\item{max_bisect}{Maximum bisection iterations (default 60)}

\item{tol}{Bracket-width convergence tolerance in delta-space (default 1e-6)}
}
\value{
Unnamed numeric vector of length 2: \code{[lower_bound, upper_bound]}
}
\description{
Used by \code{invert_test_pval_confidence_interval} (score CI and any other
CI that inverts a scalar p-value function with no derivative available).
The Wald bounds are tried first as bracket candidates before falling back
to exponential search.  Bisection then polishes to \code{tol} in delta-space.
}
